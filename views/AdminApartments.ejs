<!DOCTYPE html> 
<html lang="en"> 
<head> 
    <meta charset="UTF-8"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>Document</title> 
    <style>
        .admin-prop-cards{
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 50px;
            font-family: 'Roboto', sans-serif;
        }

        .admin-prop-card {
            width: 30rem;
            height: 36rem;
            border-radius: 10px;
            overflow: hidden;
            cursor: pointer;
            position: relative;
            box-shadow: 0 10px 30px 5px rgba(0, 0, 0, 0.2);
            margin: 0 10px;
        }

        img {
            position: absolute;
            object-fit: cover;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            transition: opacity .2s ease-out;
        }

        h2 {
            position: absolute;
            inset: auto auto 30px 30px;
            margin: 0;
            transition: inset .3s .3s ease-out;
            font-family: 'Roboto Condensed', sans-serif;
            font-weight: normal;
            text-transform: uppercase;
        }

        p, a{
            position: absolute;
            opacity: 0;
            max-width: 80%;
            transition: opacity .3s ease-out;
        }

        p {
            inset: auto auto 80px 30px;
        }

        a {
            inset: auto auto 40px 30px;
            color: inherit;
            text-decoration: none;
        }
        #del{
            position: absolute;
            display: none;
            transition: opacity .3s ease-out;
            inset:auto auto 20px 30px;
        }

        .admin-prop-card:hover h2{
            inset: auto auto 220px 30px;
            transition: inset .2s ease-out;
        }
        .admin-prop-card:hover #del{
            display: block;
        }
        .admin-prop-card:hover p, .admin-prop-card:hover a {
            opacity: 1;
            transition: opacity .4s .07s ease-in;
        }

        .admin-prop-card:hover img {
            filter: brightness(50%);
            transition: opacity .3s ease-in;
            opacity: 1;
        }

        .material-symbols-outlined {
            vertical-align: middle;
        }

        .card-content {
            color: white;
        }
        #admin-prop-add{
            display: none;
        }
    </style>

</head> 
<body> 
    <link rel="preconnect" href="https://fonts.googleapis.com"> 
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> 
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed&family=Roboto:wght@300&display=swap" rel="stylesheet"> 
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" /> 
    <div class="admin-prop-cards"></div>
    </main>
    <script src="script.js"></script>
    <div style="display:grid;justify-content:center;"><input type="button" value="add" onclick="return dis()"></div>
    <div id="admin-prop-add">
        <form id="admin-prop-myform" style="display:grid;justify-content:center;">
            Images:<input id="admin-prop-formimg" name="admin-prop-img" type="url"><br>
            Name:<input id="admin-prop-formname" name="admin-prop-name" type="text"><br>
            Details:<textarea id="admin-prop-formdetails" name="admin-prop-details" cols="20" row="4" placeholder="details"></textarea>
            <input type="button" value="add" onclick="return add()">
        </form>
    </div>
    <script>
        window.onload = function()
        {
print2();
        }
    </script>
</body>
</html>